import { Link } from 'react-router-dom';
import './Header.css';

const Header = () => {
    return(
        <header id="header">
            운동 설정 등록하기
        </header>
    )
}

export default Header;